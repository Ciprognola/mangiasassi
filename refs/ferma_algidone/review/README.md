# Review images (not shipped, not linked from the game)

Reference screenshots for owner decisions after 0.3_26. Nothing here is loaded by `index.html`.

- `bld_options.png` — `fa_bld_coccia` in-level scale: x0.5 / x0.6 (chosen) / x0.75
- `scorte_compare.png` — stock pile set A (`fa_scorte0-3`, in use) vs set B (`fa_scorte_b0-3`)
- `m4_alg.png` — Algidone angry (top girders / low stock) and eat frame
- `climb_strip.png` — climbing views: up (`fd`), still after up, down (`fu`)
