# Submissions
Each developer puts changes in `submissions/<name>/<date>/` with a `manifest.json`
(see `_template/manifest.json`) plus the audio/sprite files it references, then opens a pull request.
Submissions are NOT published. They only become part of the game once verified and hardcoded into a new build.
