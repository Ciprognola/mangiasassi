# Builds archive
Every approved version is archived here as `V<base>_<n>.html` (e.g. `V0.2_5.html`).
The newest one is also copied to `/index.html`, which is what the live link serves.
Old builds stay reachable at `.../builds/V0.2_5.html` for comparison and testing.
